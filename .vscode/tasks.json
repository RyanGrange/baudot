{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Library",
            "command": "dotnet",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        }
    ]
}
